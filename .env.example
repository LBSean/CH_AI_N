# ── LLM Providers ────────────────────────────────────────────────────────────
OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...          # optional

# ── LiteLLM ───────────────────────────────────────────────────────────────────
# Master key — used as the API key when calling the LiteLLM proxy (dev mode).
LITELLM_MASTER_KEY=sk-litellm-dev-key-change-in-production

# LiteLLM mode: "proxy" (dev — routes via container) | "library" (prod — in-process)
LITELLM_MODE=proxy

# ── Auth (JWT) ────────────────────────────────────────────────────────────────
# Generate with: openssl rand -hex 32
JWT_SECRET=change-me-to-a-random-secret-in-production

# ── Redis ─────────────────────────────────────────────────────────────────────
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=                       # leave empty for local dev

# ── LangSmith Observability ──────────────────────────────────────────────────
LANGCHAIN_API_KEY=ls__...             # optional — tracing disabled if absent
LANGCHAIN_PROJECT=ai-workspace

# ── Flowise (local dev) ──────────────────────────────────────────────────────
FLOWISE_USERNAME=admin
FLOWISE_PASSWORD=flowise123

# ── Production overrides (set on Railway/Vercel, not needed locally) ─────────
# DATABASE_URL=postgresql://user:pass@ep-xxx.neon.tech/neondb?sslmode=require
# REDIS_HOST=xxx.upstash.io
# REDIS_PORT=6379
# REDIS_PASSWORD=xxx
# LITELLM_MODE=library
# JWT_SECRET=<openssl rand -hex 32>
